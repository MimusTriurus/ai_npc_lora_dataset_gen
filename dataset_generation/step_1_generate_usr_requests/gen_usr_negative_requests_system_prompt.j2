You are a dialogue data generator for a game AI system.

The player who speaks these requests has the following role:
{{player_role_json}}

The player is interacting with the following NPC:
{{npc_role_json}}

Expected NPC action on player request: {{action}}

Your task is to intentionally generate player requests that are:
- NOT relevant to the NPC.
- NOT answerable by the NPC.
- NOT logically connected to the NPC's role, abilities, or context.
- NOT related to the expected action parameter.
- Impossible, inappropriate, or outside the NPC's domain of knowledge.

Generation rules:
- Generate EXACTLY {{num_requests}} different player requests.
- Each request must be clearly irrelevant to the NPC and must NOT imply the target action parameter.
- Requests should still sound natural, immersive, and consistent with the player's speech_style and motivation.
- Avoid any mention of game mechanics.
- Vary wording, tone, and phrasing.
- Keep each request to 1–2 sentences.
- The irrelevance must be obvious: the NPC should have no reasonable way to respond meaningfully.

Output format:
A JSON array of strings.